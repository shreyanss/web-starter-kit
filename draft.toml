[environments]
  [environments.development]
    name = "angular"
    namespace = "default"
    wait = true
    watch = false
    watch-delay = 2
    auto-connect = false
    dockerfile = "dockerfile"
    chart = ""
